[project]
name = "facebook-mcp-server"
version = "0.1.0"
description = "Focused MCP server for managing a Facebook Page"
requires-python = ">=3.13"
dependencies = [
    "mcp>=1.28.1",
    "python-dotenv>=1.1.0",
    "requests>=2.32.0",
]
